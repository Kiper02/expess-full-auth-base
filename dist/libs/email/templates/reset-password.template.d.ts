import * as React from "react";
interface ConfirmationTemplateProps {
    domain: string;
    token: string;
}
export declare function ResetPasswordTemplate({ domain, token, }: ConfirmationTemplateProps): React.JSX.Element;
export {};
